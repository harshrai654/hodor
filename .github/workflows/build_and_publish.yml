name: Publish Docker Image to GHCR (Main)

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  publish:
    name: Publish
    runs-on: arc-runner-scale-set
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        if: vars.PUSH_GHCR != 'false'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Log in to Zot
        uses: docker/login-action@v3
        with:
          registry: ${{ github.ref_name == 'main' && 'zot-internal.koinx.com' || 'zot-dev-internal.koinx.com' }}
          username: ${{ github.ref_name == 'main' && secrets.ZOT_PROD_USERNAME || secrets.ZOT_DEV_USERNAME }}
          password: ${{ github.ref_name == 'main' && secrets.ZOT_PROD_PASSWORD || secrets.ZOT_DEV_PASSWORD }}

      - name: Generate image tags
        id: tag
        shell: bash
        run: |
          set -euo pipefail
          SAFE_REF_NAME="$(echo "${GITHUB_REF_NAME}" | tr '/_' '-')"
          IMAGE_BASE="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}"
          IMAGE_BASE="$(echo "$IMAGE_BASE" | tr '[A-Z]' '[a-z]')"
          IMAGE_TAG="${SAFE_REF_NAME}-${GITHUB_SHA}"
          LATEST_REF="latest-${SAFE_REF_NAME}"

          echo "image_base=${IMAGE_BASE}" >> "$GITHUB_OUTPUT"
          echo "image_tag=${IMAGE_TAG}" >> "$GITHUB_OUTPUT"
          echo "latest_ref=${LATEST_REF}" >> "$GITHUB_OUTPUT"

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64
          push: true
          tags: |
            ${{ vars.PUSH_GHCR != 'false' && format('{0}:{1}', steps.tag.outputs.image_base, steps.tag.outputs.image_tag) || '' }}
            ${{ vars.PUSH_GHCR != 'false' && format('{0}:{1}', steps.tag.outputs.image_base, steps.tag.outputs.latest_ref) || '' }}
            ${{ vars.PUSH_GHCR != 'false' && format('{0}:latest', steps.tag.outputs.image_base) || '' }}
            ${{ github.ref_name == 'main' && 'zot-internal.koinx.com' || 'zot-dev-internal.koinx.com' }}/koinx-tech/hodor:${{ steps.tag.outputs.image_tag }}
            ${{ github.ref_name == 'main' && 'zot-internal.koinx.com' || 'zot-dev-internal.koinx.com' }}/koinx-tech/hodor:${{ steps.tag.outputs.latest_ref }}
            ${{ github.ref_name == 'main' && 'zot-internal.koinx.com' || 'zot-dev-internal.koinx.com' }}/koinx-tech/hodor:latest
          cache-from: type=registry,ref=${{ github.ref_name == 'main' && 'zot-internal.koinx.com' || 'zot-dev-internal.koinx.com' }}/koinx-tech/hodor:buildcache
          cache-to: type=registry,ref=${{ github.ref_name == 'main' && 'zot-internal.koinx.com' || 'zot-dev-internal.koinx.com' }}/koinx-tech/hodor:buildcache,mode=max
