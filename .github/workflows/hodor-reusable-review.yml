name: Hodor Reusable Review

on:
  workflow_call:
    inputs:
      pr_number:
        description: Pull request number in caller repository
        required: true
        type: string
      prompt:
        description: Optional custom prompt appended to Hodor base prompt
        required: false
        type: string
      model:
        description: LLM model to use
        required: false
        type: string
        default: anthropic/claude-sonnet-4-5-20250929
      hodor_repo:
        description: Repository containing Hodor source
        required: false
        type: string
        default: mr-karan/hodor
      kb_repo:
        description: Optional knowledge base repository slug (org/repo)
        required: false
        type: string
      kb_branch:
        description: Knowledge base branch
        required: false
        type: string
        default: main
    secrets:
      LLM_API_KEY:
        required: false
      ANTHROPIC_API_KEY:
        required: false
      OPENAI_API_KEY:
        required: false
      HODOR_KB_GITHUB_TOKEN:
        required: false

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  review:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    env:
      REPO_NAME: ${{ github.repository }}
      PR_NUMBER: ${{ inputs.pr_number }}
      LLM_MODEL: ${{ inputs.model }}
      HODOR_REPO: ${{ inputs.hodor_repo }}

    steps:
      - name: Validate required API keys
        env:
          LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          if [ -z "${LLM_API_KEY}" ] && [ -z "${ANTHROPIC_API_KEY}" ] && [ -z "${OPENAI_API_KEY}" ]; then
            echo "Error: no model API key found. Configure one of LLM_API_KEY, ANTHROPIC_API_KEY, OPENAI_API_KEY."
            exit 1
          fi

      - name: Resolve PR metadata
        id: pr
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          pr_json="$(gh api "/repos/${REPO_NAME}/pulls/${PR_NUMBER}")"
          is_fork="$(echo "${pr_json}" | jq -r '.head.repo.fork')"
          head_repo="$(echo "${pr_json}" | jq -r '.head.repo.full_name')"
          echo "is_fork=${is_fork}" >> "${GITHUB_OUTPUT}"
          echo "head_repo=${head_repo}" >> "${GITHUB_OUTPUT}"

      - name: Set up Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2

      - name: Install inspect CLI
        run: |
          set -euo pipefail
          cargo install --git https://github.com/Ataraxy-Labs/inspect inspect-cli
          inspect -h

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Clone Hodor source
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          if [ -n "${GH_TOKEN}" ]; then
            git clone "https://x-access-token:${GH_TOKEN}@github.com/${HODOR_REPO}.git" /tmp/hodor
          else
            git clone "https://github.com/${HODOR_REPO}.git" /tmp/hodor
          fi

      - name: Install and build Hodor
        working-directory: /tmp/hodor
        run: |
          set -euo pipefail
          bun install --frozen-lockfile || bun install
          bun run build

      - name: Run Hodor review
        env:
          LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          HODOR_KB_REPO: ${{ inputs.kb_repo }}
          HODOR_KB_BRANCH: ${{ inputs.kb_branch }}
          HODOR_KB_GITHUB_TOKEN: ${{ secrets.HODOR_KB_GITHUB_TOKEN }}
          HODOR_KB_WRITE_ENABLED: ${{ steps.pr.outputs.is_fork == 'true' && 'false' || 'true' }}
          HODOR_KB_PUSH_ON_SAVE: ${{ steps.pr.outputs.is_fork == 'true' && 'false' || 'true' }}
          CUSTOM_PROMPT: ${{ inputs.prompt }}
        run: |
          set -euo pipefail
          cd /tmp/hodor
          pr_url="https://github.com/${REPO_NAME}/pull/${PR_NUMBER}"
          args=(
            "dist/cli.js"
            "${pr_url}"
            "--model" "${LLM_MODEL}"
            "--post"
            "--verbose"
          )
          if [ -n "${CUSTOM_PROMPT}" ]; then
            args+=("--prompt" "${CUSTOM_PROMPT}")
          fi
          bun run "${args[@]}"

      - name: Comment on failure
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: Number(process.env.PR_NUMBER),
              body: `⚠️ Hodor review failed. Check [workflow logs](${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}).`
            });
